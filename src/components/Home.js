import Hero from './Hero';

const Home = () => {
    return (
        <div>
            <Hero text="Si quieres saber en verdad cómo una persona es, échale una mirada a cómo trata a sus inferiores, no a sus iguales" />
        </div>
    )
  }

  export default Home;